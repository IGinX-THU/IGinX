---
taskList:
- taskType: "sql"
  timeout: 10000000
  sqlList:
  - "SELECT s1, s2 FROM us.d1 WHERE key < 200;"
- taskType: "python"
  dataFlowType: "stream"
  timeout: 10000000
  pyTaskName: "AddOneTransformer"
- taskType: "sql"
  timeout: 10000000
  sqlList:
    - "SELECT s1, s1, s2 FROM addOne.us.d1;"
exportType: "none"

